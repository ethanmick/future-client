// Generated by CoffeeScript 1.9.1
(function() {
  'use strict';
  var Client, DEFAULT_PORT, Q, Socket, uuid;

  Q = require('q');

  uuid = require('uuid').v4;

  Socket = require('./socket');

  DEFAULT_PORT = 8124;

  Client = (function() {
    function Client(opts) {
      if (opts.port == null) {
        opts.port = DEFAULT_PORT;
      }
      this.socket = new Socket();
      this.socket.connect(opts);
      this.socket.on('task', (function(_this) {
        return function(task) {
          return _this.emit('task', task);
        };
      })(this));
    }

    Client.prototype.schedule = function(task) {
      return this.socket.write('create', task);
    };

    Client.prototype.get = function(name) {
      return this.socket.write(this.compress(task));
    };

    Client.prototype["delete"] = function(name) {
      return this.socket.write(this.compress(task));
    };

    return Client;

  })();

  module.exports = Client;

}).call(this);
